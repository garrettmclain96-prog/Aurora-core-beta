[project]
name = "aurora-sdk"
version = "2.0.0"
description = "Aurora Core OS — Python SDK"
requires-python = ">=3.9"
dependencies = ["requests>=2.31", "websocket-client>=1.7"]
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
